#include<stdio.h>
 main() {
 	float avg,sum=0;
 	int marks[30];
 	for(int i=0;i<=29;i++) {
 	 printf("ENTER THE MARKS:");
 	 scanf("%d",&marks);
 }
 	for(int i=0;i<=29;i++) {
 		sum=sum+marks[30];
	 }
        avg=sum/30;
	 printf("AVERAGE = %d",avg);	  	  
 	
 }
